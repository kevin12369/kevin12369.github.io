---
interface Props {
	tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-2">
	{tags.map((tag) => (
		<span
			class="rounded-full bg-stone-200 px-2 py-1 text-xs font-semibold text-stone-800 dark:bg-stone-700 dark:text-stone-200"
		>
			#{tag}
		</span>
	))}
</div>